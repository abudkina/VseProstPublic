# =============================================================================
# Requirements for Beget Hosting
# =============================================================================
# Этот файл содержит зависимости, оптимизированные для хостинга Beget.
# ML-зависимости (sentence-transformers, torch) убраны, так как они
# требуют много памяти и могут не работать на shared-хостинге.
# =============================================================================

# Core Flask Dependencies
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-CORS==4.0.0
Werkzeug==2.3.7
python-dotenv==1.0.0

# Security Dependencies
Flask-Limiter==3.5.0
Flask-WTF==1.2.1
Flask-Talisman==1.1.0
bcrypt==4.1.2
PyJWT==2.8.0
bleach==6.1.0

# Database Dependencies
PyMySQL==1.1.0
# mysqlclient не нужен на Beget - используем PyMySQL

# Redis (опционально, может не быть на Beget)
# redis==5.0.1

# API and HTTP
requests==2.31.0

# =============================================================================
# Cloud Storage Dependencies
# =============================================================================
# Yandex Object Storage (S3-compatible API)
boto3==1.34.0
botocore==1.34.0

# =============================================================================
# AI/ML Dependencies
# =============================================================================
# OpenAI API for image generation
openai==1.12.0

# =============================================================================
# ВНИМАНИЕ: ML зависимости НЕ включены!
# =============================================================================
# sentence-transformers и torch требуют много памяти и не рекомендуются
# для shared-хостинга. Система рекомендаций будет отключена.
# Если вам нужны рекомендации, рассмотрите VPS или выделенный сервер.
